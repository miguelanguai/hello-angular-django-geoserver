import { Routes } from '@angular/router';
import { CapaListComponent } from './capa/capa-list/capa-list.component';

export const routes: Routes = [
    { path: '', component: CapaListComponent },
];
